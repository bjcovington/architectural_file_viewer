// src/components/Logout.tsx
const Logout = () => {
    const handleLogout = () => {
      // Perform logout logic (e.g., clear user state, redirect, etc.)
    };
  
    return (
      <button onClick={handleLogout}>
        Logout
      </button>
    );
  };
  
  export default Logout;
  